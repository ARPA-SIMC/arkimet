## Process this file with automake to produce Makefile.in

if SQLITE_EMBEDDED
include %reldir%/sqlite/Makefile.am
endif

if LZO_EMBEDDED
include %reldir%/minilzo/Makefile.am
endif

EXTRA_DIST += %reldir%/README
