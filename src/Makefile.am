## Process this file with automake to produce Makefile.in
dist_bin_SCRIPTS = arki-query arki-scan arki-check arki-mergeconf arki-dump arki-xargs arki-server arki-testtar

if DBALLE
dist_bin_SCRIPTS += arki-bufr-prepare
endif
