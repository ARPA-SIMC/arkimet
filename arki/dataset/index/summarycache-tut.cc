#include "config.h"

#include <arki/tests/tests.h>

namespace tut {
using namespace std;

struct arki_dataset_index_summarycache_shar {
    arki_dataset_index_summarycache_shar()
    {
    }
};
TESTGRP(arki_dataset_index_summarycache);

// Trying indexing a few metadata
def_test(1)
{
}

}
