DESCRIPTION end
|\fBarki-summarise\fP reads a stream of metadata and outputs a single metadata
|item that summarises the contents of the input.

OPTIONS end
|.SH EXAMPLES
|Here are some example invocations of \fBarki-dump\fP:
|.P
|.nf
|  # Look at the summary of the contents of a grib file
|  arki-scan-grib file.grib | arki-summarise | arki-dump
|.fi
|
|.SH SEE ALSO
|.BR arki-dump (1)
